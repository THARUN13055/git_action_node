name: git_action_node
on:
  push:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: download the code
        uses: actions/checkout@v3
      - name: Install dependency
        run: npm ci
      - name: Lint code
        run: npm run lint
      - name: test the npm code
        run: npm run test
  build:
    runs-on: ubuntu-latest
    steps:
      - name: get code
        uses: actions/checkout@v3
      - name: Install dependency
        run: npm ci
      - name: build website
        run: npm run build
  deployed:
    runs-on: ubuntu-latest
    steps: 
      - name: echo the command
        run: echo "deploying ...................."
      
        
    